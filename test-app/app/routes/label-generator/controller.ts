import Controller from '@ember/controller';

import { LabelGeneratorModifier } from './demo-modifier';

export default class LabelGeneratorModifierController extends Controller {
  demo = LabelGeneratorModifier;
}
